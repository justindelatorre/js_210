/* In this exercise, you will write a program that asks the user for
 * a phrase, then outputs the number of characters in that phrase.
 */

let phrase = prompt('Please enter a phrase: ');
console.log(`There are ${phrase.length} characters in "${phrase}".`);
