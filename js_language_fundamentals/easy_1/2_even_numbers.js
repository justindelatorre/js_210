i/* Log all even numbers from 1 to 99, inclusive, to the console. Log all numbers 
 * on separate lines.
 */

for (let number = 1; number <= 99; number++) {
  if (number % 2 === 0) {
    console.log(number);
  }
}
